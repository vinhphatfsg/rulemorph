version: 2
type: network
request:
  method: GET
  url:
    - "@context.config.internal_base"
    - concat: ["/internal/api-graph"]
  headers:
    x-tenant-id: "@context.tenant_id"
timeout: 10s
internal_auth: true
