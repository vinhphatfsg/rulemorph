version: 1
input:
  format: json
mappings:
  - target: "user.id"
    source: "id"
    type: "int"
    required: true
  - target: "user.name"
    expr:
      op: "trim"
      args: [ { ref: "input.name" } ]
  - target: "meta.active"
    value: true
