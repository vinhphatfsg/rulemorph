version: 2
input:
  format: json
  json: {}
steps:
  - mappings:
      - target: "name"
        source: "input.name"
      - target: "score"
        source: "input.score"
  - record_when:
      gte: ["@out.score", 0]
finalize:
  sort:
    by: "score"
    order: "desc"
  limit: 2
